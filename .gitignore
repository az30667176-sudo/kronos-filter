# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Kronos model cache (HF)
.cache/
~/.cache/

# Vendor (cloned at setup time via git submodule)
vendor/

# Secrets
.env
.env.local
.env.*.local

# Outputs: PNG charts and CSV stay local only
output/*
!output/.gitkeep

# Results: commit all JSON reports so Vercel frontend can build from them
# (history page needs timestamped reports too)

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Frontend (set up later)
frontend/.next/
frontend/node_modules/
frontend/out/
